{
  "name": "aigb-systems",
  "version": "1.0.0",
  "description": "AIGB Systems Site",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.html dist/index.html"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  }
}
